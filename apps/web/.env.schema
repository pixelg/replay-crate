# Web app config. Only non-sensitive values may be read here; varlock refuses to
# bundle anything marked @sensitive.
#
# @import(../../, pick=[SPOTIFY_CLIENT_ID, SPOTIFY_REDIRECT_URI])
# @generateTsTypes(path=./env.d.ts)
# @defaultRequired=infer
# @defaultSensitive=false
# ---
